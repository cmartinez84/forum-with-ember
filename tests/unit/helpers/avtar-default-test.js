
import { avtarDefault } from 'question-board/helpers/avtar-default';
import { module, test } from 'qunit';

module('Unit | Helper | avtar default');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = avtarDefault([42]);
  assert.ok(result);
});

