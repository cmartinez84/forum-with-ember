
import { toStars } from 'question-board/helpers/to-stars';
import { module, test } from 'qunit';

module('Unit | Helper | to stars');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toStars([42]);
  assert.ok(result);
});

